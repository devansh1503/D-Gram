To use this Chat application:
1. Open terminal in your editor
2. write the following commands:
cd backend
nodemon main
3.Now you can open the live server from the HTML file of the project for first user, and open another window with same url for second user.
